# Signing secrets — never commit keystores or passwords
*.jks
*.keystore
*.p12
pixelfit-upload.jks
**/local.properties
keystore.properties
key.properties
secrets.properties

# Android Studio — local IDE state (JDK, last-run device, caches)
.idea/
*.iml
